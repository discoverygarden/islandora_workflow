<?php
/**
 * @file
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

/**
 * Does nothing.
 *
 * @param int $tt
 *   A Time Trial number. Always equals one (1).
 *
 * @return int
 *   An integer value, equal to one (1).
 */
function test_you($tt = 1) {
  $a = 1;
  return $a;
}
